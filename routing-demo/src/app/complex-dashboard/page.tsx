export default function ComplexDashboardPage() {
    return (
        <div>
            <h1>Complex Dashboard</h1>
            <p>This dashboard has multiple nested components that fetch data independently.</p>
        </div>
    );
}